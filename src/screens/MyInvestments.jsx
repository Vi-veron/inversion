const { Typography } = require("@material-ui/core");

function MyInvestments() {
  return (
    <div>
      <Typography>
        This page should contain a list of all the investments the user has
        made.
      </Typography>
    </div>
  );
}
export default MyInvestments;
